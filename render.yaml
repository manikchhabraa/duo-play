services:
  - type: web
    name: duo-play
    runtime: node
    plan: free
    buildCommand: npm install && node scripts/make-icons.mjs && npm run build
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: "22"
